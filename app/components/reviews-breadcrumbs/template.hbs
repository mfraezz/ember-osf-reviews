{{#each breadcrumbs as |breadcrumb|}}
    {{#if (eq breadcrumb breadcrumbs.lastObject)}}
        <span local-class="current-page">{{breadcrumb.name}}</span>
    {{else}}
        {{#link-to breadcrumb.path}}
            {{breadcrumb.name}}
        {{/link-to}}
        <span local-class="separator"></span>
    {{/if}}
{{/each}}
